
import 'package:flutter/material.dart';

class vocabulary_screen extends StatelessWidget {
  const vocabulary_screen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Saved words page"),
    );
  }
}